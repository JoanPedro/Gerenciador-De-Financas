# import * from './Operation.graphql'

mutation Categorycreate(
  $description: String!
  $operation: Operation!
) {
  createCategory(
    description: $description,
    operation: $operation
  ){
    id
    description
    operation
    user {
      id
      name
      email
    }
  }
}